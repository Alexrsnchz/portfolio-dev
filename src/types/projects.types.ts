export interface ProjectProps {
  thumbnail: string;
  title: string;
  description: string;
  stack: string[];
  source: string;
  sourceAria: string;
  sourceButton: string;
  preview: string;
  previewAria: string;
  previewButton: string;
  reverse?: boolean;
}
